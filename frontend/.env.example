REACT_APP_SUPABASE_URL="YOUR_SUPABASE_URL_HERE"
REACT_APP_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY_HERE"

# Instructions:
# 1. Create a new file named .env.local in this directory (frontend/)
# 2. Copy the content of this file (.env.example) into .env.local
# 3. Replace the placeholder values with your actual Supabase Project URL and Anon Key.
#    You can find these in your Supabase project settings under "API".
#
# DO NOT commit your actual keys to version control.
# The .env.local file is already included in .gitignore to prevent accidental commits.
